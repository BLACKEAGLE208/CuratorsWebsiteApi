using System.Reflection.Metadata;
using System.Xml.Linq;
using CuratorsWebApi.Models;
using Microsoft.EntityFrameworkCore;
//using System.Data.Entity;

namespace SwaggerTest
{
    /*class CuratorsDB
    {
        static void ReadAndWrite()
        {
            using (CuratorsContext db = new CuratorsContext())
            {
                Curator curator1 = new Curator { FirstName = "Magomed Magomedov" };
                Curator curator2 = new Curator { FirstName = "Budaychi Budaychievich" };
            
                db.Curators.Add(curator1);
                db.Curators.Add(curator2);
                db.SaveChanges();

                var curators = db.Curators;

                foreach(Curator c in curators)
                {
                    Console.WriteLine(c.FirstName);
                    Console.WriteLine(c.LastName);
                }
            }
        }
    }*/
}

